# No runtime URL or target env vars.
# Form factor is detected at runtime (desktop vs mobile).
# Service addresses are constants: http://yaad.dadi, http://dimaag.dadi, http://nas.dadi.
# Control URL and auth come from the provisioning bundle, not env.
