/** House refetch cadence for local mesh reads (Nas / Yaad / agent logs). */
export const POLL_MS = 2_000;
/** Virtual-display frame cadence for the view-only browser preview. */
export const FRAME_MS = 1_500;
