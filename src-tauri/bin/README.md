# Built by `net/build-tailscale.sh` — not committed.
# Expected layout:
#   darwin-arm64/{tailscale,tailscaled}
#   darwin-amd64/{tailscale,tailscaled}
#   linux-amd64/{tailscale,tailscaled}
#   windows-amd64/{tailscale.exe,tailscaled.exe,wintun.dll}
